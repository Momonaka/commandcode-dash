# Bilingual-pair consistency record, same shape as the DSH monorepo's package
# READMEs: the git blob hash of each side as of the last confirmed-consistent
# state. Both languages carry equal authority; after editing either side, bring
# the other along and re-record with:
#   git hash-object README.md README.zh.md
# (The monorepo's `verify-translation-pairing` script lives with that workspace
# and does not travel with this out-of-tree plugin, so the hashes are recorded
# by hand here.)
README.md: 92d2f3e00ee4219b33b5828601f978adb8632ea4
README.zh.md: 154999de940cc42570047389e75b0a3db7aaa066
