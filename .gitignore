# Dependencies — this plugin has none, but a local install may create one.
node_modules/

# macOS
.DS_Store

# Logs and scratch output
*.log

# Agent working notes: kept in the checkout, never part of the published repo.
AGENT.md
